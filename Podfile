# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Recency' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Recency
  pod 'SwiftLint'

  target 'RecencyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RecencyUITests' do
    # Pods for testing
  end

end
